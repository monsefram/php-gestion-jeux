<?php
require_once "config.php";
require_once "session.php";
require_once "section/entete.phtml";


$utilisateurDao = new UtilisateurDao($config);
$utilisateurs = $utilisateurDao->selectAll(); // méthode à avoir dans UtilisateurDao
?>

<!DOCTYPE html>
<html lang="fr">

<head>
    <?php require_once "section/head.phtml"; ?>
    <title>Utilisateurs - Point Final</title>
</head>

<body>
    <?php require_once "section/entete.phtml"; ?>

    <div class="container mt-5">
        <h2 class="mb-4 text-center">Liste des utilisateurs</h2>

        <div class="row justify-content-center">
            <?php foreach ($utilisateurs as $u): ?>
                <div class="col-md-4 col-lg-3 mb-4">
                    <div class="card shadow-sm h-100 text-center">
                        <img src="<?= htmlspecialchars($u->getUrlAvatar()) ?>"
                            class="card-img-top"
                            alt="Avatar de <?= htmlspecialchars($u->getNomUtilisateur()) ?>">

                        <div class="card-body">
                            <h5 class="card-title"><?= htmlspecialchars($u->getNomUtilisateur()) ?></h5>
                            <p class="card-text text-muted"><?= htmlspecialchars($u->getRole()->getNom()) ?></p>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    </div>

    <?php require_once "section/pied.phtml"; ?>
</body>

</html>