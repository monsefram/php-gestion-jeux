<?php
require_once "config.php";
require_once "session.php";

$statDao = new StatistiqueDao($config);
$classement = $statDao->selectClassementGlobal(); // méthode existante normalement
?>

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Projet Modifié</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/assets/css/nouveau-style.css" rel="stylesheet">
</head>

<body id="classement">
    <?php require_once "section/entete.phtml"; ?>

    <div class="container mt-5">
        <h2 class="text-center mb-4">Classement</h2>

        <div class="table-responsive">
            <table class="table table-hover table-bordered align-middle text-center">
                <thead class="table-light">
                    <tr>
                        <th>Rang</th>
                        <th>Joueur</th>
                        <th>Gagnées</th>
                        <th>Égalité</th>
                        <th>Perdues</th>
                        <th>Total</th>
                        <th>Score</th>
                    </tr>
                </thead>
                <tbody>
                    <?php
                    $rang = 1;
                    foreach ($classement as $c): ?>
                        <tr>
                            <td><?= $rang++ ?></td>
                            <td><?= htmlspecialchars($c->getNomComplet()) ?></td>
                            <td><?= $c->getNbPartiesGagnees() ?></td>
                            <td><?= $c->getNbPartiesEgales() ?></td>
                            <td><?= $c->getNbPartiesPerdues() ?></td>
                            <td><?= $c->getNbPartiesJouees() ?></td>
                            <td class="fw-bold"><?= $c->getScoreTotal() ?></td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>

    <?php require_once "section/pied.phtml"; ?>
</body>
</html>
