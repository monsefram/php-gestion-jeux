<?php
require_once "config.php";
require_once "session.php";
require_once "dal/dao/UtilisateurDao.php";
require_once "dal/dao/StatistiqueDao.php";
require_once "dal/dao/PartieDao.php";

if (!isset($_GET["id"]) || !is_numeric($_GET["id"])) {
    header("Location: index.phtml");
    exit;
}

$id = intval($_GET["id"]);
$utilisateurDao = new UtilisateurDao($config);
$statDao = new StatistiqueDao($config);
$partieDao = new PartieDao($config);

$utilisateur = $utilisateurDao->select($id);
$stat = $statDao->selectClassementGlobalParJoueur($id);
$parties = $partieDao->selectParJoueur($id);

if (!$utilisateur) {
    echo "<div class='container mt-5 alert alert-danger'>Utilisateur introuvable</div>";
    exit;
}
?>

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Profil de <?= htmlspecialchars($utilisateur->getNomUtilisateur()) ?></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/assets/css/nouveau-style.css" rel="stylesheet">
</head>
<body>
<?php require_once "section/entete.phtml"; ?>

<div class="container py-5">
  <div class="row mb-5">
    <div class="col-md-4 text-center">
      <img src="<?= htmlspecialchars($utilisateur->getUrlAvatar()) ?>" class="rounded-circle mb-3" width="200">
      <h4><?= htmlspecialchars($utilisateur->getPrenom()) ?> <?= htmlspecialchars($utilisateur->getNom()) ?> <small class="text-muted">(<?= htmlspecialchars($utilisateur->getNomUtilisateur()) ?>)</small></h4>
      <p class="text-muted"><?= nl2br(htmlspecialchars($utilisateur->getBio())) ?></p>
      <p><strong>Membre depuis :</strong> <?= $utilisateur->getDateCreation()->format("Y-m-d") ?></p>
      <p><strong>Type de compte :</strong> <?= $utilisateur->getRole()->getNom() ?></p>
    </div>

    <div class="col-md-8">
      <h5 class="mb-3">Statistiques</h5>
      <?php if ($stat): ?>
        <ul class="list-group">
          <li class="list-group-item">Victoires : <?= $stat->getNbPartiesGagnees() ?></li>
          <li class="list-group-item">Égalités : <?= $stat->getNbPartiesEgales() ?></li>
          <li class="list-group-item">Défaites : <?= $stat->getNbPartiesPerdues() ?></li>
          <li class="list-group-item">Parties jouées : <?= $stat->getNbPartiesJouees() ?></li>
          <li class="list-group-item fw-bold">Score total : <?= $stat->getScoreTotal() ?></li>
        </ul>
      <?php else: ?>
        <p class="text-muted">Aucune statistique disponible.</p>
      <?php endif; ?>
    </div>
  </div>

  <hr>

  <h4 class="mb-4">Toutes les parties jouées</h4>
  <div class="row">
    <?php foreach ($parties as $p): ?>
      <div class="col-md-6 col-lg-4 mb-4">
        <div class="card text-center shadow-sm">
          <div class="card-body">
            <div class="d-flex justify-content-around align-items-center mb-2">
              <div>
                <img src="<?= $p->getJoueur1()->getUrlAvatar() ?>" class="rounded-circle mb-1" width="50">
                <div><?= htmlspecialchars($p->getJoueur1()->getNomUtilisateur()) ?></div>
                <div class="fw-bold"><?= $p->getScoreJoueur1() ?></div>
              </div>
              <div class="fs-3 fw-bold">VS</div>
              <div>
                <img src="<?= $p->getJoueur2()->getUrlAvatar() ?>" class="rounded-circle mb-1" width="50">
                <div><?= htmlspecialchars($p->getJoueur2()->getNomUtilisateur()) ?></div>
                <div class="fw-bold"><?= $p->getScoreJoueur2() ?></div>
              </div>
            </div>
            <div class="mt-2">
              <img src="<?= $p->getJeu()->getUrlImageJeu() ?>" style="height: 40px;" alt="Jeu">
              <div class="text-muted small">Ajouté le <?= $p->getDateCreation()->format("Y-m-d à H:i") ?></div>
            </div>
          </div>
        </div>
      </div>
    <?php endforeach; ?>
  </div>
</div>

<?php require_once "section/pied.phtml"; ?>
</body>
</html>
