<?php
require_once "config.php";
require_once "session.php";
require_once "section/entete.phtml";


$partieDao = new PartieDao($config);
$parties = $partieDao->selectAll(); // suppose que cette méthode existe
?>

<!DOCTYPE html>
<html lang="fr">
<head>
    <?php require_once "section/head.phtml"; ?>
    <title>Liste des parties - Point Final</title>
</head>
<body>
    <?php require_once "section/entete.phtml"; ?>

    <div class="container mt-4">
        <h2 class="mb-4">Liste des parties jouées</h2>

        <table class="table table-bordered table-hover table-striped">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Date</th>
                    <th>Joueur 1</th>
                    <th>Score J1</th>
                    <th>Joueur 2</th>
                    <th>Score J2</th>
                    <th>Jeu</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($parties as $partie) : ?>
                    <tr>
                        <td><?= htmlspecialchars($partie->getId()) ?></td>
                        <td><?= htmlspecialchars($partie->getDateCreation()->format('Y-m-d H:i')) ?></td>
                        <td><?= htmlspecialchars($partie->getJoueur1()->getNomUtilisateur()) ?></td>
                        <td><?= htmlspecialchars($partie->getScoreJoueur1()) ?></td>
                        <td><?= htmlspecialchars($partie->getJoueur2()->getNomUtilisateur()) ?></td>
                        <td><?= htmlspecialchars($partie->getScoreJoueur2()) ?></td>
                        <td><?= htmlspecialchars($partie->getJeu()->getNom()) ?></td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>

    <?php require_once "section/pied.phtml"; ?>
</body>
</html>
