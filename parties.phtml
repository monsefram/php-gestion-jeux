<?php
require_once "config.php";
require_once "session.php";
require_once "dal/dao/PartieDao.php";

require_once "session.php";

require_once "session.php";

$utilisateur = getUtilisateurConnecte();

if (!$utilisateur) {
    header("Location: connexion.phtml");
    exit;
}

if (!$utilisateur) {
    header("Location: connexion.phtml");
    exit;
}
$dao = new PartieDao($config);
$parties = $dao->selectAll();
?>

<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <title>Liste des parties</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/assets/css/nouveau-style.css" rel="stylesheet">
</head>

<body>
    <?php require_once "section/entete.phtml"; ?>

    <div class="container py-5">
        <h1 class="mb-4">Historique des parties</h1>

        <?php if (count($parties) === 0): ?>
            <div class="alert alert-info">Aucune partie enregistrée.</div>
        <?php endif; ?>

        <div class="row row-cols-1 row-cols-md-2 g-4">
            <?php foreach ($parties as $p): ?>
                <div class="col">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="text-center">
                                    <a href="profil.phtml?id=<?= $p->getJoueur1()->getId() ?>">
                                        <img src="<?= $p->getJoueur1()->getUrlAvatar() ?>" class="rounded-circle" width="50">
                                    </a>
                                    <p class="mb-0"><?= htmlspecialchars($p->getJoueur1()->getNomUtilisateur()) ?></p>
                                    <strong><?= $p->getScoreJoueur1() ?></strong>
                                </div>

                                <div class="fs-4 fw-bold">VS</div>

                                <div class="text-center">
                                    <a href="profil.phtml?id=<?= $p->getJoueur2()->getId() ?>">
                                        <img src="<?= $p->getJoueur2()->getUrlAvatar() ?>" class="rounded-circle" width="50">
                                    </a>
                                    <p class="mb-0"><?= htmlspecialchars($p->getJoueur2()->getNomUtilisateur()) ?></p>
                                    <strong><?= $p->getScoreJoueur2() ?></strong>
                                </div>
                            </div>

                            <div class="mt-3 text-center">
                                <img src="<?= $p->getJeu()->getUrlImageJeu() ?>" style="height: 40px;">
                                <div class="text-muted small">Joué le <?= $p->getDateCreation()->format("Y-m-d H:i") ?></div>
                            </div>

                            <?php if ($utilisateurConnecte && $utilisateurConnecte->getRole()->getNom() === "Administrateur"): ?>
                                <form method="post" action="supprimer-partie.php" onsubmit="return confirm('Supprimer cette partie ?');" class="text-end mt-3">
                                    <input type="hidden" name="id_partie" value="<?= $p->getId() ?>">
                                    <button class="btn btn-sm btn-danger">🗑 Supprimer</button>
                                </form>
                            <?php endif; ?>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    </div>

    <?php require_once "section/pied.phtml"; ?>
</body>

</html>